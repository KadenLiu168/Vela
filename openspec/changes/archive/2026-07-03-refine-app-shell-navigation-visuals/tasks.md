## 1. App Shell Hooks

- [x] 1.1 Add stable class names for the brand block, API metadata, nav container, and nav links in `AppShell.tsx`.
- [x] 1.2 Preserve nav labels, hrefs, `aria-current`, and `onNavigate` behavior unchanged.

## 2. Header Navigation Styling

- [x] 2.1 Restyle the header brand, API metadata, nav pill container, nav links, and active state with existing design tokens.
- [x] 2.2 Add responsive rules so the header and pill navigation remain readable on narrow viewports.

## 3. Validation

- [x] 3.1 Run OpenSpec validation for the change and specs.
- [x] 3.2 Run existing frontend validation commands: lint, typecheck, test, and build.
- [x] 3.3 Run a browser smoke check for the default route.
