## 1. Web App Package Setup

- [x] 1.1 Create `apps/web` npm package files for Vite, React, TypeScript, linting, tests, and build scripts.
- [x] 1.2 Add TypeScript, Vite, ESLint, and Vitest configuration scoped to `apps/web`.

## 2. Web App Skeleton

- [x] 2.1 Create the Vite HTML entrypoint and React source entrypoint.
- [x] 2.2 Add `src/pages`, `src/components`, `src/api`, and `src/test` structure with minimal skeleton code.
- [x] 2.3 Add a frontend test covering the rendered skeleton.

## 3. Documentation and Validation

- [x] 3.1 Document root-level and app-local web development commands in `apps/web/README.md`.
- [x] 3.2 Update repository documentation only where needed to acknowledge the new web app skeleton.
- [x] 3.3 Run frontend validation, existing backend validation, and OpenSpec validation.
