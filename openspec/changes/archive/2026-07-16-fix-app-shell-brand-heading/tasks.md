## 1. AppShell Brand Semantics

- [x] 1.1 Change the AppShell banner brand from a heading element to non-heading visible text using `app-brand-title`
- [x] 1.2 Move the existing brand typography styles from the structural header heading selector to `.app-brand-title`

## 2. Accessibility Tests

- [x] 2.1 Update or add route rendering tests that assert each AppShell page exposes exactly one `<h1>`
- [x] 2.2 Add assertions that the `Vela Research` banner brand remains visible and is not rendered as any heading element

## 3. Verification

- [x] 3.1 Run the relevant frontend test suite for AppShell and route heading behavior
- [x] 3.2 Run OpenSpec validation for `fix-app-shell-brand-heading`
