## 1. Configuration Schema & Validation

- [x] 1.1 Change `DefenseConfig.asset` to `DefenseConfig.assets: list[ETFIdentity]` with `Field(min_length=1)` in `strategy_config.py`.
- [x] 1.2 Add a `model_validator` rejecting duplicate `(exchange, symbol)` entries in `assets`.
- [x] 1.3 Update `_validate_defensive_asset` to loop over all `defense.assets`, verifying each is an active ETF in the universe; error message references `defense.assets[i]`.
- [x] 1.4 Update `config/strategy_v1.yaml`: replace `defense.asset` with `defense.assets` (a list with at least one entry). Configure **three** defensive assets in order: `SSE 511010`, `SSE 511880`, `SSE 518880` (国债ETF / 银华日利ETF / 黄金ETF). Each receives equal weight `Decimal("1") / Decimal(3)` at fallback.
  Loader-validation dependency (`_validate_defensive_asset` checks `config/etf_pool.yaml`, NOT the DB): all three already exist in `config/etf_pool.yaml` with `is_active: true` - verified - so `load_strategy_config` passes with **no change to `etf_pool.yaml`**.
  Runtime-resolution dependency (`defense_lookup` is built from the DB's active ETF rows at backtest time): the only integration test loading the real `strategy_v1.yaml` and running the backtest is `apps/api/tests/test_backtest_run.py`, whose temp DB currently `add_etf`s only `510300`/`159915`/`511010`. You MUST also `add_etf` `511880` and `518880` there (both `exchange="SSE", currency="CNY"`), in BOTH test functions (`test_run_backtest_endpoint_runs_core_workflow_and_persists_results` and `test_run_backtest_endpoint_updates_backtest_detail`) - see 5.6.
  Recommended: also call `_add_price_history` for the two new ETFs (mirror `defense`'s `daily_step=Decimal("0.01")`). The two risky ETFs (`510300`+`159915`) already satisfy `top_n=2` so this test does NOT normally trigger fallback and `defense_lookup` is not exercised on the happy path - but adding price history keeps the new ETFs as fully-formed active universe members and avoids any dependence on empty-price-panel edge behavior.
  Implementation watchpoint: `test_backtest_run.py` asserts exact values (`max_drawdown == "0.000000"`, `signal_count == 2`, `total_return is not None`, `trading_day_count == 10`). Adding two active ETFs that enter `active_etfs` (even without price data) could shift these if any downstream calculation iterates `active_etfs`. Run the backtest test first after 1.4+5.6; if exact assertions drift, adjust the expected values (NOT the scenario) to match the still-successful backtest.
  `tests/integration_data.py` needs NO change - `seed_minimal_workflow_data` does not load `strategy_v1.yaml` or build `defense_lookup`.

## 2. Selection Logic

- [x] 2.1 Update `select_with_defensive_fallback` to return one `DefensiveFallbackSelection` per configured defensive asset when `len(rankings) < top_n`, each with target weight `1 / N` (N = number of assets).
- [x] 2.2 Verify the single-asset degenerate case yields weight `1.0` (unchanged behavior).
- [x] 2.3 Verify the sum of fallback target weights is within rounding tolerance for N > 1: `abs(sum(weights) - Decimal("1")) < Decimal("1e-9")` (matching `ScoreWeightsConfig`). Do NOT assert exact equality, since `Decimal("1") / Decimal(N)` is a repeating Decimal for N=3, 6, … and the rounded sum is approximately, not exactly, `1.0`.

## 3. Signal Generation Resolution

- [x] 3.1 Update the failure path in `generate_strategy_signal` so the "defensive asset not found" message names the specific missing `(exchange, symbol)` — i.e. the `DefensiveFallbackSelection` whose `defense_lookup` lookup returned `None` (use `selection.exchange` / `selection.symbol`, not `config.defense.assets`). If more than one defensive asset is missing, the first one encountered in `defense.assets` configuration order triggers the failure and is the one named. The message always refers to one concrete asset; it must NOT reference the whole `defense.assets` list generically or use a generic plural.
- [x] 3.2 Confirm `_to_generated_position` resolves each fallback selection via the existing `defense_lookup` (no change to `defense_lookup` construction in `packages/core/src/vela_core/backtest_runner.py`, `apps/api/src/vela_api/main.py`, or `apps/cli/src/vela_cli/main.py`).

## 4. API Serialization & Web Display

- [x] 4.1 Confirm `apps/api/src/vela_api/config.py` `_serialize_config` emits `defense.assets` via `defense.model_dump()` (no logic change expected).
- [x] 4.2 Update the `DashboardResponse.defense` type in `apps/web/src/api/client.ts` from `{ asset: { exchange, symbol } }` to `{ assets: { exchange, symbol }[] }` (the `DashboardResponse` type lives in `client.ts`, not `DashboardPage.tsx`). Then update `apps/web/src/pages/DashboardPage.tsx`: change the `formatDefensiveAsset` input type from `DashboardResponse["strategy"]["defense"]["asset"]` to the list shape, rewrite the helper to render the list (e.g. join `exchange:symbol` entries), and update the call site `data.strategy.defense.asset` to `data.strategy.defense.assets`.
- [x] 4.3 Update every typed `DashboardResponse` mock that still uses `defense: { asset: { exchange, symbol } }` so it compiles against the new `defense.assets` list shape: `apps/web/src/api/client.test.ts`, `apps/web/src/App.test.tsx`, `apps/web/src/components/CommandPalette.test.tsx`, `apps/web/src/components/CommandPalette.stories.tsx` (each mock's `defense` field becomes `{ assets: [ { exchange, symbol } ] }`). These are TypeScript-typed mocks; leaving them on `asset` fails the web type-check.

## 5. Tests

- [x] 5.1 Update `test_strategy_config.py`: `assets=[...]`, uniqueness rejection, empty-list rejection, active/universe validation messages reference `defense.assets[i]`.
- [x] 5.2 Update `test_momentum_scoring.py`: fallback returns N entries with equal weights; sum within `1e-9` tolerance (`abs(sum-1) < 1e-9`, NOT exact equality for N > 1); single-asset keeps `1.0` exactly.
- [x] 5.3 Update `test_strategy_signal_generation.py`: the `_strategy_config` helper builds `StrategyConfig` via `model_validate` over a dict, so move its `defense` dict key from `{"asset": {...}}` to `{"assets": [...]}`. Add a multi-asset case (two configured defensive assets) and assert the failure message names the single specific missing `(exchange, symbol)` of the `DefensiveFallbackSelection` whose `defense_lookup` lookup returned `None` (not a generic plural). Include a case where the first asset resolves and the second is absent from `defense_lookup`, asserting the message names the second asset's `(exchange, symbol)`.
- [x] 5.4 Update `test_bootstrap.py` and `apps/api/tests/test_bootstrap_endpoint.py` to `assets=[...]`.
- [x] 5.5 `tests/integration_data.py` needs NO change for `defense.assets`: `seed_minimal_workflow_data` does not load `strategy_v1.yaml`, does not run signal generation, and does not build `defense_lookup` - it inserts pre-built `StrategySignal`/`BacktestRun` ORM rows for dashboard-aggregation tests. The `SSE 511010` it seeds via `add_etf` is incidental (a referenced ETF row), not a `defense.assets` dependency. (The real `defense.assets` data-source sync points, if `strategy_v1.yaml` adds a second asset, are `config/etf_pool.yaml` for loader validation and `apps/api/tests/test_backtest_run.py` for runtime `defense_lookup` - see 1.4.)
- [x] 5.6 `packages/core/tests/test_backtest_runner.py`: the `_strategy_config` helper builds `StrategyConfig` via `model_validate` over a dict - move its `defense` dict key from `{"asset": {...}}` to `{"assets": [...]}` with the same three assets as `strategy_v1.yaml` (`511010`, `511880`, `518880`). `apps/api/tests/test_backtest_run.py`: this test does NOT construct `DefenseConfig` or `defense_lookup`; it seeds a temp DB via `add_etf` and runs the backtest through the API (which loads the real `strategy_v1.yaml`). Per 1.4, `add_etf` `511880` and `518880` (`exchange="SSE", currency="CNY"`) in BOTH test functions, alongside the existing `511010`. Recommended: mirror the existing `defense` block with `_add_price_history(..., daily_step=Decimal("0.01")`) for each new ETF. If exact backtest assertions (`max_drawdown`, `signal_count`, `total_return`) drift after adding the two active ETFs, adjust the expected values to match the still-successful run - do NOT weaken the scenario.
- [x] 5.7 Update `apps/api/tests/test_api_config.py`: the `/api/config` response assertion moves `defense` from `{"asset": {"exchange": "SSE", "symbol": "511010"}}` to `{"assets": [{"exchange": "SSE", "symbol": "511010"}]}` (the endpoint serializes the new `DefenseConfig.assets` list).
- [x] 5.8 Update `packages/core/tests/test_trend_filter.py`: the `_strategy_config` helper builds `StrategyConfig` via `model_validate` over a dict - move its `defense` dict key from `{"asset": {...}}` to `{"assets": [...]}` (the helper only needs a valid `defense` to satisfy the schema; it does not exercise fallback).
- [x] 5.9 Update `packages/core/tests/test_strategy_equity_curve.py`: the `_strategy_config` helper builds `StrategyConfig` via `model_validate` over a dict - move its `defense` dict key from `{"asset": {...}}` to `{"assets": [...]}` (same as 5.8; the helper only needs a valid `defense` to satisfy the schema).

## 6. Validation

- [x] 6.1 Run core momentum-scoring, strategy-config, and signal-generation tests.
- [x] 6.2 Run API, CLI, and integration tests.
- [x] 6.3 Run lint, formatting check, and `openspec validate support-multiple-defensive-assets`.
