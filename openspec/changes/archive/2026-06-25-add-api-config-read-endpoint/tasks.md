## 1. API Config Tests

- [x] 1.1 Add failing integration tests for `GET /api/config` using the real checked-in strategy config and ETF pool.
- [x] 1.2 Update API route surface tests to include `GET /api/config`.

## 2. API Config Endpoint

- [x] 2.1 Add API config response shaping that uses `vela_core.load_app_config`.
- [x] 2.2 Add the read-only `GET /api/config` route.

## 3. Documentation and Validation

- [x] 3.1 Document the config endpoint.
- [x] 3.2 Run API tests, full backend validation, and OpenSpec validation.
