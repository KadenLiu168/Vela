## 1. Test Suite Validation

- [x] 1.1 Run `uv run pytest` from the repository root and capture the result.
- [x] 1.2 If pytest fails, identify the failing tests and apply the smallest scoped fix needed for COP-75.

## 2. Final Verification

- [x] 2.1 Run the existing lint, format, type check, and OpenSpec validation commands that apply to this repository.
- [x] 2.2 Confirm the final change only covers COP-75 and does not include unrelated code changes.
