## 1. Signal Detail Visual Refinement

- [x] 1.1 Refine `.detail-page` metadata and `.compact-list` styling using existing design tokens without changing JSX behavior.
- [x] 1.2 Refine `.holdings-section`, `.holdings-table-wrap`, and `.holdings-table` styling for editorial table readability, numeric columns, row separation, and horizontal-scroll container visuals.
- [x] 1.3 Confirm empty states remain consistent and no API, route, business logic, dependency, sorting, filtering, pagination, or positions data rendering changes were introduced.

## 2. Validation

- [x] 2.1 Run OpenSpec validation for `refine-signal-detail-editorial-table`.
- [x] 2.2 Run supported frontend validation commands: `npm run lint`, `npm run typecheck`, `npm run test`, and `npm run build` from `apps/web`, or document unavailable scripts.
- [x] 2.3 Complete review/fix/validate loop and record final findings.
