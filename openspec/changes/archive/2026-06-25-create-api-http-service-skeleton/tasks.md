## 1. API Package Setup

- [x] 1.1 Add FastAPI and uvicorn dependencies.
- [x] 1.2 Add `apps/api/src` to package discovery and register the `vela-api` console script.

## 2. API Skeleton

- [x] 2.1 Add failing API tests for importing the app and calling `GET /api/health`.
- [x] 2.2 Implement the minimal FastAPI app and health route.
- [x] 2.3 Implement the `vela-api` startup command.

## 3. Documentation and Validation

- [x] 3.1 Document the API startup command and health endpoint.
- [x] 3.2 Run API tests, full backend validation, and OpenSpec validation.
