## 1. Token Foundation

- [x] 1.1 Add the provided color, typography, spacing, layout, radius, and surface custom properties to `apps/web/src/styles.css`.
- [x] 1.2 Align global base rules for box sizing, body typography, text color, and page background with the token custom properties.

## 2. Safe Style Wiring

- [x] 2.1 Replace direct global stylesheet values with token references where there is a clear one-to-one mapping to the provided design tokens.
- [x] 2.2 Preserve existing route structure, component DOM, API behavior, and unmatched semantic state colors.

## 3. Validation

- [x] 3.1 Run OpenSpec validation for the change and specs.
- [x] 3.2 Run existing frontend validation commands: lint, typecheck, test, and build.
