## 1. API Client Contract

- [x] 1.1 Add failing frontend tests for successful JSON responses, HTTP errors, and network errors.
- [x] 1.2 Implement the shared API client and endpoint helper needed to pass the client tests.

## 2. Frontend Integration

- [x] 2.1 Add failing frontend behavior coverage for calling API health through the shared client from the app UI.
- [x] 2.2 Wire the app UI and Vite local proxy so frontend code uses the shared client for the health request.

## 3. Validation

- [x] 3.1 Add and run a local API integration validation path that performs a real request against the FastAPI service.
- [x] 3.2 Run frontend tests, frontend lint/typecheck/build, applicable backend validation, and OpenSpec validation.
