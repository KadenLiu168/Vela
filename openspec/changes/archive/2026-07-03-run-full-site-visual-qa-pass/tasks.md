## 1. Explore and Scope

- [x] 1.1 Read COP-136 context, `DESIGN.md`, active tokens/styles, and relevant Dashboard, Signal Detail, Backtest Detail, AppShell, and spec files.
- [x] 1.2 Record auto decisions for any missing token artifacts or ambiguous visual QA scope.

## 2. Visual Consistency Pass

- [x] 2.1 Scan `apps/web/src/styles.css` for hardcoded colors, blue/green palette remnants, heavy font weights, shadows, and inconsistent radius/spacing on scoped surfaces.
- [x] 2.2 Apply minimal CSS fixes for token consistency and desktop/mobile readability without changing business logic, API calls, routes, or dependencies.

## 3. Review and Validation

- [x] 3.1 Run lint, typecheck, test, build, and OpenSpec validation commands supported by the repository.
- [x] 3.2 Manually inspect `/`, `/signals/demo-signal`, and `/backtests/1` at desktop and mobile viewport sizes.
- [x] 3.3 Complete up to five review/fix/validate loops and record final blocker/major/minor findings.
