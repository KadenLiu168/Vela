# dashboard-grid-3-col-balance

Rebalance the dashboard grid from 4 columns to 3 so Signal, Backtest, and Fetches each occupy one equal column. Removes the empty trailing column slot in the bottom row.
