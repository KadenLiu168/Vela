# JoinQuant (jqdatasdk) credentials — required only when using the JoinQuant
# market data provider. Copy this file to `.env` (gitignored) and fill in values.
# NEVER commit real credentials. The `config/*.yaml` files are git-tracked and
# must not hold secrets.
JQ_USERNAME=
JQ_PASSWORD=
